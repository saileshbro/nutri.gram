import 'package:flutter/material.dart';

class AdviceView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Advice View'),
    );
  }
}
